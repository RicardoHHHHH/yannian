# 研念验证记录

版本：0.8.0。验证日期：2026-09-09。主要环境：Windows、本地 Python 与 Node.js。

0.8.0 增加跨平台启动与通用 API 接入。本机完整后端 136 项、前端 38 项、公式 8 项、PDF 坐标 / 布局 / 生命周期及启动脚本语法检查通过；此前 PDF 离线绘图与下载实测按原版本保留。GitHub Actions 的平台结果以对应提交的实际运行记录为准。

## 自动化检查

以下是本机检查结果；跨平台 GitHub CI 结果见下一节。

| 检查 | 结果与覆盖 |
| --- | --- |
| 完整后端回归 | 136 项通过；含 21 项 API 接入测试、5 项跨平台启动检查、39 项 PDF 下载 / 导入回归、已有文献 / 对话 / 选区 / 发现 / Codex 及打包覆盖 |
| 发布打包检查 | 1 项通过；检查源码白名单、个人数据与环境排除、隐藏配置模板和第三方许可证保留 |
| 前端源码单元检查 | 38 项通过；新增 DeepSeek 预设、切换服务清除输入密钥、图片 / 联网能力提示、思考档位和按地址隔离模型偏好；保留此前阅读与对话覆盖 |
| 公式渲染 | 8 项通过；使用实际 Marked + KaTeX，包括反馈中的中文分段函数、行内与独立公式、矩阵环境、流式未完成公式、代码和价格保留、无效语法回退及字体文件完整性 |
| PDF 坐标检查 | 缩放、反向拖选、边界裁剪与高亮位置通过 |
| PDF 页面布局检查 | 4 倍/6 倍采样、图块像素完整覆盖、巨大页面只分配可见区域且不降采样、连续页边界、500 页按需窗口与 400% 缩放位置通过 |
| PDF 阅读器生命周期检查 | 25 页 DOM / PDF 替身；新增横向滚动的图块加载/释放、400% 缩放不降采样和绘图坐标验证；像素密度变化、阅读位置、选区取消、连续阅读、返回引用与失败重试通过 |
| 脚本语法 | 本次修改的 JavaScript 已执行语法 / 源码检查；Bash 校验 `start.sh` / `start.command`、PowerShell 解析 `start.ps1` 通过 |
| 干净源码包 | 源码文件与 ZIP 内容一致；16 个 HTML 依赖资源完整；包含 KaTeX 脚本、样式、60 个字体文件及许可证 |
| 本机升级 | 服务返回研念 0.8.0 标识；原有文献、项目、idea、对话及选区记录保持一致 |

测试命令见 [开发说明](CONTRIBUTING.md)。后端存在一条第三方 Starlette / anyio 的弃用警告，不影响以上断言结果。

发布材料已检查仓库内文档链接、机器路径与凭据形态；打包目录不含本地资料库、论文原文件、`.env`、缓存或运行环境。用于宣传的使用者供图包含画面中可见的文献条目、项目名与 idea 卡片。未来向仓库添加文件后仍需重新检查。

后端测试使用独立临时数据库、合成 PDF 和模拟外部服务，不访问个人 Zotero 库，不发送真实模型请求。前端单元检查使用 DOM 替身，不等于真实浏览器的视觉与鼠标交互验收。

## 0.8.0 跨平台安装与启动

提交 `4ad6a12` 的 [GitHub Actions 实际运行](https://github.com/RicardoHHHHH/yannian/actions/runs/34334310989) 全部通过：

| 平台 | Python | 结果 |
| --- | --- | --- |
| Windows（windows-latest） | 3.11 | 后端、前端、首次环境安装、PowerShell 快速启动、服务复用与打包通过 |
| macOS 15 / Apple Silicon（macos-15） | 3.13 | 后端、前端、首次环境安装、Bash 快速启动、服务复用与打包通过 |
| macOS 15 / Intel（macos-15-intel） | 3.11 | 后端、前端、首次环境安装、Bash 快速启动、服务复用与打包通过 |
| Linux（ubuntu-latest） | 3.13 | 后端、前端、首次环境安装、Bash 快速启动、服务复用与打包通过 |

另在使用者的 Windows 安装目录实测：新版安装器检查并登记已有依赖约 3.24 秒，随后通过 PowerShell 入口复用正在运行的服务约 0.75 秒；这不是新机器首次下载依赖的耗时。原 PDF 文件逐字节一致，原有资料与对话在升级时核对保持一致，Codex 连接仍可用。

CI 使用临时机器和模拟模型服务，未使用个人资料或付费模型请求。该结果覆盖终端启动与后台服务，不代表已人工验收 Finder / Gatekeeper、Safari 排版和各屏幕的鼠标操作。

## 0.7.5 高清分块渲染检查

用项目实际的 PDF.js 6.3.289 与独立测试用 `@napi-rs/canvas`，离线读取前次下载的公开 EMNLP 论文第 1、4 页。两页都以 4 倍采样输出 2,400 × 3,395 像素，分别比较整页画布与四块拼接结果，包括边界附近文字和图形。测试断言像素最大通道差超过 8 的像素少于 0.01%，平均通道差低于 0.03；两页通过。Canvas 仅供测试使用，应用未增加运行依赖。

这是真实 PDF 的离线绘图验证，尚未覆盖浏览器的 CSS 合成与鼠标视觉验收。复现命令见 [开发说明](CONTRIBUTING.md)。

## 0.7.4 下载耗时实测

在同一台 Windows 机器上，先后下载使用者反馈的 [EMNLP 2025 PDF](https://aclanthology.org/2025.emnlp-main.142.pdf)。只保存至临时验证目录，未加入个人文献库。

| 项目 | 普通下载 | 四路分段下载 |
| --- | --- | --- |
| 文件大小 | 11,871,278 字节 | 11,871,278 字节 |
| 传输耗时（包括连接） | 109.453 秒 | 36.460 秒 |
| 本地解析 | 0.040 秒 | 0.045 秒 |
| 解析结果 | 12 页 / 267 个文字块 | 12 页 / 267 个文字块 |

两份文件 SHA-256 均为 `7a1590a1109bb98e4842a323a674211dfed44a2df9a5fdd9ba96939b6e1eb610`。这组观测约提速 3 倍；是顺序进行的单次对比，不排除网络、缓存和来源负载变化，不代表所有网址都具有相同收益。来源提供 `Accept-Ranges: bytes` 与强 ETag；下载无需模型调用。

## Codex 真实连接与停止

另使用独立临时资料库、合成测试问题完成两次真实 Codex 请求，未使用个人论文：

- 成功获取 7 个本机可用模型及其思考档位、图像支持能力。
- GPT-6 Astra 以 low 档位完成简短回答，约 4.51 秒。
- 第二轮接续同一对话；检测到回答文本后调用停止接口，约 0.05 秒确认停止，保留部分文本与全部 4 条历史消息，后台执行锁已释放。

以上是一次本机观测，不保证网络延迟和未来模型目录不变。停止通过官方 App Server `turn/interrupt` 接口实现，后端会等候工作线程清理。没有展示或持久化原始推理内容。

## 公开 PDF 的真实下载验证

网址导入在 0.6.0 完成以下真实网络验证；改名版本保留同一下载实现，相关接口回归已再次通过。验证使用临时资料库。

| 输入 | 验证结果 |
| --- | --- |
| [MEDAGENTS 的 ACL PDF 直链](https://aclanthology.org/2024.findings-acl.33.pdf) | 下载并解析 23 页、492 个文字块；识别完整多行标题，添加项目，重复导入复用 |
| [Learning Latent Dynamics for Planning from Pixels 的 PMLR 网页](https://proceedings.mlr.press/v97/hafner19a.html) | 从网页发现公开 PDF；下载并解析 11 页、462 个文字块，添加项目，重复导入复用 |

这验证了一个 PDF 直链和一个论文落地页，不代表所有出版平台可访问，也不覆盖登录、付费墙和验证码。

## 宣传截图

README 中的三张展示图以项目使用者提供的截图为基础，使用内置 ImageGen 编辑，清理豆包悬浮头像、白色圆框及半圆残影，并修整边缘、留白、圆角与阴影。它们是经过修图的宣传素材，并非逐像素保留的原始截图。

已逐张查看主要标题、按钮、文献条目与 idea 卡片，检查悬浮窗和边缘残影是否移除；同时检查 PNG 完整性、README 图片路径及源码包内容。静态图检查不等同于浏览器操作回归验证，也未展示 PDF 选区问答或 idea 分析报告。

## 尚未覆盖

- 本次升级未完成真实浏览器中的清晰度对比、分块合成和屏幕切换验收；离线绘图和源码单元检查不替代浏览器视觉验证。
- macOS 的 Finder 双击、Gatekeeper 提示、Safari 实际阅读以及移动设备尚未进行人工验收；终端启动与后台服务由跨平台 CI 检查。
- DeepSeek / 通用 API 的请求格式、流式中断、图像能力和密钥隔离通过模拟服务测试，未发送真实付费 API 请求；其他服务与 Codex 版本仍可能存在协议差异。
- 尚无大规模文献库性能基准；检索召回率、idea 创新性和生成结论不由这些工程测试保证。
