# 研念验证记录

版本：0.7.4。验证日期：2026-09-09。主要环境：Windows、本地 Python 与 Node.js。

0.7.4 重跑 PDF 来源、网址导入与打包测试（合计 40 项），以及前端单元、PDF 页面布局、阅读器生命周期和相关 JavaScript 语法检查；使用反馈中的公开 PDF 实测下载耗时与文件完整性。本次未改动的其他后端全量回归与 PDF 坐标结果沿用 0.7.1，公式渲染结果沿用 0.7.2。

## 自动化检查

以下结果来自本地执行，不表示已在 GitHub CI 或全部平台上通过。

| 检查 | 结果与覆盖 |
| --- | --- |
| PDF 下载与导入回归 | 本次 39 项通过；包括四路并发与字节重组、文件标识/范围/长度异常时回退、弱标识禁用分段、进度与解析阶段、失败终态、原有网址获取和数据保留；其他后端测试未重跑 |
| 发布打包检查 | 1 项通过；检查源码白名单、个人数据与环境排除、隐藏配置模板和第三方许可证保留 |
| 前端源码单元检查 | 36 项通过；新增实际下载量/速度/用时展示、解析阶段与传输区分；保留 Esc、图片附件、历史、草稿、分栏和模型控制覆盖 |
| 公式渲染 | 8 项通过；使用实际 Marked + KaTeX，包括反馈中的中文分段函数、行内与独立公式、矩阵环境、流式未完成公式、代码和价格保留、无效语法回退及字体文件完整性 |
| PDF 坐标检查 | 缩放、反向拖选、边界裁剪与高亮位置通过 |
| PDF 页面布局检查 | 新增 1 倍/3 倍屏幕的采样分辨率、20 MP 与最大边长限制；连续页边界、500 页按需渲染、混合页尺寸及缩放位置通过 |
| PDF 阅读器生命周期检查 | 25 页 DOM / PDF 替身；新增像素密度变化后的画布重绘、旧画布释放、准确保留滚动位置和监听清理；原有选区取消、连续阅读、返回引用与重试通过 |
| JavaScript 语法 | 本次 `static/app.js`、`static/pdf-reader.mjs`、`static/pdf-layout.mjs` 通过；`static/assistant.js`、`static/math.js` 沿用 0.7.2 检查 |
| 干净源码包 | 源码文件与 ZIP 内容一致；16 个 HTML 依赖资源完整；包含 KaTeX 脚本、样式、60 个字体文件及许可证 |
| 本机升级 | 服务返回研念 0.7.4 标识；原有文献、项目、idea、对话及选区记录保持一致 |

测试命令见 [开发说明](CONTRIBUTING.md)。后端存在一条第三方 Starlette / anyio 的弃用警告，不影响以上断言结果。

发布材料已检查仓库内文档链接、机器路径与凭据形态；打包目录不含本地资料库、论文原文件、`.env`、缓存或运行环境。用于宣传的使用者供图包含画面中可见的文献条目、项目名与 idea 卡片。未来向仓库添加文件后仍需重新检查。

后端测试使用独立临时数据库、合成 PDF 和模拟外部服务，不访问个人 Zotero 库，不发送真实模型请求。前端单元检查使用 DOM 替身，不等于真实浏览器的视觉与鼠标交互验收。

## 本次下载耗时实测

在同一台 Windows 机器上，先后下载使用者反馈的 [EMNLP 2025 PDF](https://aclanthology.org/2025.emnlp-main.142.pdf)。只保存至临时验证目录，未加入个人文献库。

| 项目 | 普通下载 | 四路分段下载 |
| --- | --- | --- |
| 文件大小 | 11,871,278 字节 | 11,871,278 字节 |
| 传输耗时（包括连接） | 109.453 秒 | 36.460 秒 |
| 本地解析 | 0.040 秒 | 0.045 秒 |
| 解析结果 | 12 页 / 267 个文字块 | 12 页 / 267 个文字块 |

两份文件 SHA-256 均为 `7a1590a1109bb98e4842a323a674211dfed44a2df9a5fdd9ba96939b6e1eb610`。这组观测约提速 3 倍；是顺序进行的单次对比，不排除网络、缓存和来源负载变化，不代表所有网址都具有相同收益。来源提供 `Accept-Ranges: bytes` 与强 ETag；下载无需模型调用。

## Codex 真实连接与停止

另使用独立临时资料库、合成测试问题完成两次真实 Codex 请求，未使用个人论文：

- 成功获取 7 个本机可用模型及其思考档位、图像支持能力。
- GPT-6 Astra 以 low 档位完成简短回答，约 4.51 秒。
- 第二轮接续同一对话；检测到回答文本后调用停止接口，约 0.05 秒确认停止，保留部分文本与全部 4 条历史消息，后台执行锁已释放。

以上是一次本机观测，不保证网络延迟和未来模型目录不变。停止通过官方 App Server `turn/interrupt` 接口实现，后端会等候工作线程清理。没有展示或持久化原始推理内容。

## 公开 PDF 的真实下载验证

网址导入在 0.6.0 完成以下真实网络验证；改名版本保留同一下载实现，相关接口回归已再次通过。验证使用临时资料库。

| 输入 | 验证结果 |
| --- | --- |
| [MEDAGENTS 的 ACL PDF 直链](https://aclanthology.org/2024.findings-acl.33.pdf) | 下载并解析 23 页、492 个文字块；识别完整多行标题，添加项目，重复导入复用 |
| [Learning Latent Dynamics for Planning from Pixels 的 PMLR 网页](https://proceedings.mlr.press/v97/hafner19a.html) | 从网页发现公开 PDF；下载并解析 11 页、462 个文字块，添加项目，重复导入复用 |

这验证了一个 PDF 直链和一个论文落地页，不代表所有出版平台可访问，也不覆盖登录、付费墙和验证码。

## 宣传截图

README 中的三张展示图以项目使用者提供的截图为基础，使用内置 ImageGen 编辑，清理豆包悬浮头像、白色圆框及半圆残影，并修整边缘、留白、圆角与阴影。它们是经过修图的宣传素材，并非逐像素保留的原始截图。

已逐张查看主要标题、按钮、文献条目与 idea 卡片，检查悬浮窗和边缘残影是否移除；同时检查 PNG 完整性、README 图片路径及源码包内容。静态图检查不等同于浏览器操作回归验证，也未展示 PDF 选区问答或 idea 分析报告。

## 尚未覆盖

- 本次升级未完成真实浏览器中的清晰度对比、屏幕切换及下载进度视觉验收；源码单元检查不替代鼠标与视觉验证。
- macOS / Linux 启动、移动设备和各浏览器组合尚未实测。
- API 模式的取消与模型参数通过实现和接口测试覆盖，未发送真实付费 API 请求；其他 Codex 版本仍可能存在协议差异。
- 尚无大规模文献库性能基准；检索召回率、idea 创新性和生成结论不由这些工程测试保证。
